{
    "mcpServers": {
      "firecrawl": {
        "baseUrl": "https://mcp.firecrawl.dev/v2/mcp",
        "description": "Firecrawl hosted MCP for scraping and extraction",
        "headers": {
          "Authorization": "Bearer ${FIRECRAWL_API_KEY}"
        }
      }
    },
    "imports": []
  }